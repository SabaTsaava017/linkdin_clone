import { ButtonDiv, NewChatButton } from "@/app/styles/chat-styled/styled"

export const StartChatButton = () =>{
    return(
        <ButtonDiv>
            <NewChatButton>START NEW CHAT</NewChatButton>
        </ButtonDiv>
    )
}